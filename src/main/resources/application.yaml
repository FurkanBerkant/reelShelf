spring:
  application:
    name: reelshelf

  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:postgresql://localhost:5432/demo_db
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver